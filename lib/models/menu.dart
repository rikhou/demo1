import 'package:flutter/material.dart';

class MenuItem {
  const MenuItem(this.id, this.title, this.icon, this.route);
  final String id;
  final String title;
  final IconData icon;
  final String route;
}
